# StockLedger Desktop Scaffold

This folder is reserved for the PC client shell.

Planned stack:
- Electron
- SQLite offline event store
- Local outbox sync worker
- Shared client validation from a future `packages/client-core`

Current status: scaffold only. The web prototype remains the active UI while workflows are being finalized.
